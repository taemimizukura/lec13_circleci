---
- name: Check if Rails is installed
  shell: |
    bash -lc "/home/ec2-user/.rbenv/shims/gem list rails -i"
  register: rails_installed
  ignore_errors: yes

- name: Install Rails
  shell: bash -lc "gem install rails -v 7.1.3.2 --no-document"
  when: rails_installed is failed


